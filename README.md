# flutter-django-api
Connects the flutter android frontend to django backend
# API
This uses RESTful API. The major features in the project are POST, GET, PUT, DELETE. 
Uses Django Restframework (DRF) 
